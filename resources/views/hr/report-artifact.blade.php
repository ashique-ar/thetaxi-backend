<!doctype html><html><head><meta charset="utf-8"><style>body{font-family:DejaVu Sans,sans-serif;font-size:9px;color:#111}h1{font-size:16px}table{width:100%;border-collapse:collapse}th,td{border:1px solid #bbb;padding:4px;vertical-align:top;word-break:break-word}th{background:#eee}</style></head><body><h1>{{ $title }}</h1><table><thead><tr>@foreach($columns as $column)<th>{{ $column }}</th>@endforeach</tr></thead><tbody>@foreach($rows as $row)<tr>@foreach($columns as $column)<td>{{ is_array($row[$column]??null)||is_object($row[$column]??null) ? json_encode($row[$column], JSON_UNESCAPED_SLASHES) : ($row[$column]??'') }}</td>@endforeach</tr>@endforeach</tbody></table></body></html>
