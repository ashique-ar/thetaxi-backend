meta {
  name: Deactivate Device
  type: http
  seq: 5
}

post {
  url: {{base_url}}/api/driver/devices/{{device_uuid}}/deactivate
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
}

docs {
  Deactivate a specific device.
}
