meta {
  name: Get Current Assignment
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/driver/assignments/current
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
}

docs {
  Get the current active assignment (placeholder for future implementation).
}
